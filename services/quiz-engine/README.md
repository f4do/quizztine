# Quiz Engine (archived)

The quiz-engine was originally a standalone Python FastAPI service.
It was ported to TypeScript and integrated in-process into the web-backend
(`services/web-backend/src/engine/`) to simplify deployment.

This directory is preserved for reference only. It is no longer used in the
active codebase or Docker Compose configuration.
