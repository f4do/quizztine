# Frontend dev (standalone) — Vite dev server on :5173, backend on :3000
# In production (mono-container via docker-compose.prod.yml), these should be:
#   VITE_API_URL=/api
#   VITE_SOCKET_URL=/
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
VITE_DEFAULT_LANG=fr
