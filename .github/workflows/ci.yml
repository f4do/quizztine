name: CI

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
    branches: [master]

env:
  NODE_VERSION: '24'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: |
            services/web-backend/pnpm-lock.yaml
            services/frontend/pnpm-lock.yaml
      - name: Install backend dependencies
        run: pnpm install
        working-directory: services/web-backend
      - name: Generate Prisma client
        run: pnpm prisma generate
        working-directory: services/web-backend
      - name: Install frontend dependencies
        run: pnpm install --ignore-scripts
        working-directory: services/frontend

      - name: Lint frontend (prettier)
        run: pnpm prettier --check src/
        working-directory: services/frontend
      - name: Lint backend (eslint)
        run: pnpm lint
        working-directory: services/web-backend

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: |
            services/web-backend/pnpm-lock.yaml
            services/frontend/pnpm-lock.yaml
      - name: Install backend dependencies
        run: pnpm install
        working-directory: services/web-backend
      - name: Generate Prisma client
        run: pnpm prisma generate
        working-directory: services/web-backend
      - name: Install frontend dependencies
        run: pnpm install --ignore-scripts
        working-directory: services/frontend

      - name: Typecheck frontend
        run: pnpm typecheck
        working-directory: services/frontend
      - name: Typecheck backend
        run: pnpm typecheck
        working-directory: services/web-backend

  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: services/web-backend/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install
        working-directory: services/web-backend
      - name: Generate Prisma client
        run: pnpm prisma generate
        working-directory: services/web-backend
      - name: Run tests
        run: pnpm test
        working-directory: services/web-backend
        env:
          JWT_SECRET: ci-secret

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: |
            services/web-backend/pnpm-lock.yaml
            services/frontend/pnpm-lock.yaml
      - name: Install backend dependencies
        run: pnpm install
        working-directory: services/web-backend
      - name: Generate Prisma client
        run: pnpm prisma generate
        working-directory: services/web-backend
      - name: Install frontend dependencies
        run: pnpm install --ignore-scripts
        working-directory: services/frontend

      - name: Build backend
        run: pnpm build
        working-directory: services/web-backend
      - name: Build frontend
        run: pnpm build
        working-directory: services/frontend

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: services/frontend/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --ignore-scripts
        working-directory: services/frontend
      - name: Run tests
        run: pnpm test
        working-directory: services/frontend

  docker:
    runs-on: ubuntu-latest
    needs: [build, test-backend, test-frontend]
    permissions:
      contents: write
      packages: write

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: quizztine
          POSTGRES_PASSWORD: quizztine
          POSTGRES_DB: quizztinedb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and load Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: quizztine:ci-test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test — start container and wait for healthy
        run: |
          docker run -d --name quizztine-smoke \
            --network host \
            -e DATABASE_URL=postgresql://quizztine:quizztine@localhost:5432/quizztinedb \
            -e JWT_SECRET=ci-smoke-secret \
            -e NODE_ENV=production \
            quizztine:ci-test

          for i in $(seq 1 30); do
            status=$(docker inspect --format='{{.State.Health.Status}}' quizztine-smoke 2>/dev/null || echo "starting")
            echo "[$i/30] health: $status"
            case "$status" in
              healthy)
                echo "✅ Container is healthy — app started, DB connected"
                docker stop quizztine-smoke
                exit 0
                ;;
              unhealthy)
                echo "❌ Container is unhealthy"
                docker logs quizztine-smoke
                docker stop quizztine-smoke
                exit 1
                ;;
            esac
            sleep 2
          done

          echo "❌ Timeout — container never became healthy"
          docker logs quizztine-smoke
          docker stop quizztine-smoke
          exit 1

      - name: Tag, push, and release
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"
          MINOR="$(echo $VERSION | cut -d. -f1-2)"
          docker tag quizztine:ci-test ghcr.io/f4do/quizztine:${VERSION}
          docker tag quizztine:ci-test ghcr.io/f4do/quizztine:${MINOR}
          docker tag quizztine:ci-test ghcr.io/f4do/quizztine:latest
          docker push --all-tags ghcr.io/f4do/quizztine
        shell: bash

      - name: Generate release notes
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          name: Quizztine ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
